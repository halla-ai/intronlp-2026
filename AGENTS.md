# Agent Guide

언어모형과 자연어처리(131107967A) 2026-2 강의 저장소. Astro 6 + Starlight, GitHub Pages 배포.

## 정본

**수업계획서 정본은 이 저장소에 없다.** 학사시스템 등록본이 정본이며 work repo
`admin/depts/ai/courses/2026-2/`에서 관리한다. 이 저장소의
`src/content/docs/syllabus.md` 는 **학생용 파생본**이다. 여기서 고치지 않는다.

## 실습 설계 원칙 (반드시 지킬 것)

노트북은 **골격 제공 + TODO 한 지점**이다.

- 동작하는 코드를 먼저 주고, 학생은 표시된 한 지점만 바꾼다
- **밑바닥부터 작성하게 하지 않는다.** 라이브러리 사용을 막지 않는다
- **TODO를 건드리지 않은 상태로도 노트북이 끝까지 실행돼야 한다**
- Colab 무료 티어 규모. GPU 의존 금지
- 매 주차 돌아가는 결과물이 나와야 한다

이 원칙은 수업계획서에서 온 것이며 임의로 바꾸지 않는다.

## 콘텐츠 규칙

- 특정 상용 제품·구독 서비스 이름을 넣지 않는다. 개념·원리와 기반 라이브러리까지만
- 예제는 제주 공개 데이터에서 가져온다(`src/content/docs/reference/data.md`)
- **도내 기업 데이터는 없다.** 기업은 상정 조건으로만 등장시킨다
- 확인하지 않은 논문·수치·인용을 만들어 내지 않는다
- 학생 폴더에는 학번만. 이름·연락처를 넣지 않는다

## 문서 스타일

- 개조식(명사형 종결). 기호 사다리 □ → ○ → - → ·
- ASCII 문장부호. em-dash·굽은 따옴표·줄임표 문자를 쓰지 않는다

## 작업

```bash
make install
make dev
make build
make status
make new-week N=07
make new-notebook N=07
```

## 커밋

- 커밋 메시지는 영문
- 과제 제출 PR(`assignments/**`)과 자료 수정 PR을 섞지 않는다
